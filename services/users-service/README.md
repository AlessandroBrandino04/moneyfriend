# Users Service

Minimal scaffolding for users and groups management.

Commands:

```powershell
cd services/users-service
npm install
npm run prisma:generation
npm run prisma:migrate
npm run dev
```

Environment variables are in `.env.example`.
