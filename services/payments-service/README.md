# Payments Service

Minimal scaffolding for payments/expenses management.

Commands:

```powershell
cd services/payments-service
npm install
npm run prisma:generation
npm run prisma:migrate
npm run dev
```

Environment variables are in `.env.example`.
